{%- liquid
  assign s = section.settings
  assign product = s.product
-%}

<section class="aurora aurora-hero" style="{% render 'aurora-spacing', settings: section.settings %}">
  <div class="aurora__inner aurora-hero__inner">
    <div class="aurora-hero__copy">
      {%- if s.eyebrow != blank -%}
        <p class="aurora__eyebrow aurora-reveal">{{ s.eyebrow }}</p>
      {%- endif -%}

      <h1 class="aurora__display aurora-hero__title aurora-reveal" style="--aur-reveal-delay: 80ms">
        {{ s.heading }}
      </h1>

      {%- if s.subheading != blank -%}
        <p class="aurora__lede aurora-reveal" style="--aur-reveal-delay: 160ms">{{ s.subheading }}</p>
      {%- endif -%}

      <div class="aurora-hero__action aurora-reveal" style="--aur-reveal-delay: 240ms">
        <a class="aurora__btn aurora__btn--primary" id="aurora-hero-cta" href="{{ s.cta_link | default: routes.all_products_collection_url }}">
          {{ s.cta_label }}
          {%- if product != blank and s.show_price -%}
            <span class="aurora__price">{{ product.price | money }}</span>
          {%- endif -%}
        </a>

        {%- if s.assurance != blank -%}
          <p class="aurora-hero__assurance">{{ s.assurance }}</p>
        {%- endif -%}
      </div>
    </div>

    <div class="aurora-hero__media aurora-reveal" style="--aur-reveal-delay: 120ms">
      {%- if s.image != blank -%}
        {{
          s.image
          | image_url: width: 1400
          | image_tag:
            widths: '500, 800, 1100, 1400',
            sizes: '(max-width: 989px) 100vw, 48vw',
            loading: 'eager',
            fetchpriority: 'high',
            alt: s.image.alt | default: s.heading | strip_html
        }}
      {%- else -%}
        <img
          src="{{ 'aurora-hero-rings.jpg' | asset_url }}"
          width="1200"
          height="1262"
          loading="eager"
          fetchpriority="high"
          alt="Three Aurora rings in black, rose gold and copper finishes"
        >
      {%- endif -%}
    </div>
  </div>
</section>

{% stylesheet %}
  .aurora-hero {
    padding-block: var(--padding-block-start, 0) var(--padding-block-end, 0);
  }

  .aurora-hero__inner {
    display: grid;
    grid-template-columns: 1fr;
    gap: clamp(1.75rem, 5vw, 5rem);
    align-items: center;
    padding-block: clamp(1.5rem, 8vh, 6rem);
  }

  .aurora-hero__media img,
  .aurora-hero__media svg {
    display: block;
    width: 100%;
    height: auto;
  }

  /* --- MOBILE STYLES: Reverting to the Dark Luxury Theme --- */
  @media screen and (max-width: 989px) {
    .aurora-hero__inner {
      position: relative;
      display: flex;
      flex-direction: column;
      min-height: 85vh;
      padding-block: 0;
    }

    .aurora-hero__media {
      position: absolute;
      inset: 0;
      width: 100%;
      height: 100%;
      z-index: 0;
    }

    .aurora-hero__media img,
    .aurora-hero__media svg {
      width: 100%;
      height: 100%;
      object-fit: cover;
      object-position: center;
    }

    /* The Dark Scrim Overlay (Crucial for white text readability) */
    .aurora-hero__media::after {
      content: '';
      position: absolute;
      display: block;
      inset: 0;
      background: linear-gradient(
        180deg, 
        rgba(20, 21, 26, 0.2) 0%, 
        rgba(20, 21, 26, 0.55) 45%, 
        rgba(20, 21, 26, 0.85) 100%
      );
      pointer-events: none;
    }

    .aurora-hero__copy {
      position: relative;
      z-index: 1;
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      padding: 12vh 1.5rem 2rem; /* Sets top spacing so button lands naturally lower */
      height: 100%;
    }

    .aurora__eyebrow {
      color: rgba(255, 255, 255, 0.7) !important;
      font-size: 0.75rem;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      margin-bottom: 1rem;
    }

    .aurora-hero__title {
      color: #ffffff !important;
      font-size: clamp(2.25rem, 9vw, 2.75rem);
      line-height: 1.1;
      text-shadow: none !important;
      margin: 0;
      max-width: 14ch;
    }

    .aurora__lede {
      color: rgba(255, 255, 255, 0.85) !important;
      font-size: 0.9375rem;
      line-height: 1.5;
      margin-top: 1.5rem; /* Restores the gap between heading and subheading */
      text-shadow: none !important;
      font-weight: 400;
      max-width: 90%;
    }

    .aurora-hero__action {
      margin-top: 2.5rem; /* Pushes the button down to roughly align below the black ring */
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    /* Optional: Make button look sleek on dark background */
    .aurora__btn--primary {
      background: #ffffff;
      color: #14151a;
      border: none;
    }

    .aurora-hero__assurance {
      margin: 0;
      font-size: 0.75rem;
      color: rgba(255, 255, 255, 0.6) !important;
    }
  }

  /* --- DESKTOP STYLES --- */
  .aurora-hero__title {
    max-width: 12ch;
  }

  .aurora-hero__action {
    margin-top: clamp(2rem, 4vw, 3rem);
  }

  .aurora-hero__assurance {
    margin: 0.875rem 0 0;
    font-size: 0.8125rem;
    color: var(--aur-text-muted);
  }

  .aurora-hero__placeholder {
    background: var(--aur-page-alt);
    fill: rgba(20, 21, 26, 0.12);
  }

  @media screen and (min-width: 990px) {
    .aurora-hero__inner {
      grid-template-columns: 0.9fr 1.1fr;
      gap: clamp(2.5rem, 5vw, 5rem);
      align-items: start;
      padding-block: var(--aur-hero-top) var(--aur-hero-bottom);
    }

    .aurora-hero__copy {
      padding-block-start: var(--aur-hero-top);
    }

    .aurora-hero__media {
      margin-right: 0;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Aurora hero",
  "tag": "section",
  "class": "aurora-section",
  "disabled_on": { "groups": ["header", "footer"] },
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "The smart ring without the subscription" },
    {
      "type": "inline_richtext",
      "id": "heading",
      "label": "Heading",
      "default": "Sleep is the <em>whole</em> game."
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "Aurora measures your nights and tells you one thing each morning: push, or hold back. Buy it once and every insight is yours for good."
    },
    { "type": "image_picker", "id": "image", "label": "Product image" },
    { "type": "header", "content": "Action" },
    { "type": "text", "id": "cta_label", "label": "Button label", "default": "Buy Aurora" },
    { "type": "url", "id": "cta_link", "label": "Button link" },
    { "type": "product", "id": "product", "label": "Product (for live price)" },
    { "type": "checkbox", "id": "show_price", "label": "Show price in button", "default": true },
    {
      "type": "text",
      "id": "assurance",
      "label": "Assurance line",
      "default": "Free Sizing Kit Included · 30-Day Risk-Free Trial · 2-Year Warranty"
    },
    { "type": "header", "content": "Padding" },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "Top",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "Bottom",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 24
    }
  ],
  "presets": [{ "name": "Aurora hero", "category": "Aurora" }]
}
{% endschema %}